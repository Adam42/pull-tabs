var pullTabsApp = pullTabsApp || {};
pullTabsApp.Config = pullTabsApp.Config || (function () {

    var setup = {
        "credentials" : {
            "consumer_key": ""
        },

        "configuration" : {
            "mode": "PRODUCTION"
        },

        "app": {
            "name": "Pull Tabs"
        }
    };

    return setup;

}(pullTabsApp));